[project]
name = "remanence-lib-workspace"
version = "0"
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["crates/remanence-py"]

[dependency-groups]
testing-prep = [
    "reliquary==0.1.0a1",
]
